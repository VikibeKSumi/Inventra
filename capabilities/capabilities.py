


class CapabilityService():

    def get_product(self, sku: str):
        ...


        data = {
            "sku": sku,
            "name": name,
            "category": category,
            "active": active,
        }
        suc
        output = ToolResult(
            success=success,
            code=code,
            message=message,
            evidence=evidece
            data=data,
        )
        return output